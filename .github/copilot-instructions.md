Make sure to only use public APIs that do not require usernames, registrations or tokens.

When adding new capabilities, try to write code that abides by the SOLID principles aand is easily testable. 

Make sure that calls to the environment and external APIs are isolated and passed into calling functions with dependency injection, so that they can be easily stubbed or mocked.

In all interactions, refer to me as "Captain".